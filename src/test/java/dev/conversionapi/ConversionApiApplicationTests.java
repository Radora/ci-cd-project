package dev.conversionapi;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ConversionApiApplicationTests {

    /*
    @Test
    void createNewApiObject() {
        MutableConversionAPI converter = new MutableConversionAPI();
        converter.setId(UUID.randomUUID().toString());
        converter.settCelsius(25.3f);
        converter.setWeightKilogramm(87);

        String msg = "Success, converter object is not empty!";


        Assert.notNull(converter, msg);
    }

     */

}
